# Pages Directory
#
# This empty directory exists for compatibility with older versions of Next.js
# that check for a `pages/` folder on startup.
#
# DLE Entertainment uses the App Router (`app/` directory).
# Do NOT add page routes here — put them in `app/` instead.
